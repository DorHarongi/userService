export class ResourcesAmounts
{
    constructor(public woodAmount: number, public stonesAmount: number, public cropAmount: number)
    {
        this.woodAmount = woodAmount;
        this.stonesAmount = stonesAmount;
        this.cropAmount = cropAmount;
    }
}
