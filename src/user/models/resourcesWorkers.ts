export class ResourcesWorkers
{
    constructor(public woodWorkers: number, public stoneWorkers: number, public cropWorkers: number)
    {
        this.woodWorkers = woodWorkers;
        this.stoneWorkers = stoneWorkers;
        this.cropWorkers = cropWorkers;
    }
}